# Core requirements (always needed)
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
Pillow>=9.0.0
matplotlib>=3.5.0

# Bayesian optimization (highly recommended)
scikit-optimize>=0.9.0

# Scientific computing (recommended)
scipy>=1.7.0
scikit-learn>=1.0.0

# Visualization (recommended)
seaborn>=0.12.0

# Image processing (optional but useful)
scikit-image>=0.19.0
# opencv-python>=4.5.0  # Only if using advanced image processing

# Optional dependencies for advanced similarity metrics
# Install these manually if needed:
#
# For perceptual similarity (LPIPS):
# pip install lpips>=0.1.4
#
# For semantic similarity (CLIP):
# pip install git+https://github.com/openai/CLIP.git
#
# For PDF report generation:
# pip install reportlab>=3.6.0

# Note: ComfyUI and its dependencies should already be installed